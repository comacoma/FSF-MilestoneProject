language: python
python:
    - "3.6"
services: sqlite3
env:
    matrix:
        - DJANGO=2.0 DB=sqlite3
install:
    - "pip install -q Django==$DJANGO"
    - "pip install -q -r requirements.txt"
script:
- ./manage.py test
